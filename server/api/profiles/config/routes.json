{
  "routes": [
    {
      "method": "GET",
      "path": "/profiles/:id",
      "handler": "profiles.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/profiles/:id",
      "handler": "profiles.update",
      "config": {
        "policies": []
      }
    }
  ]
}
